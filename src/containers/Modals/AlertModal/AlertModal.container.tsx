import { type FC } from "react";
import AlertModalPresenter from "./presenter/AlertModal.presenter";

const AlertModalContainer: FC = () => {
  return <AlertModalPresenter />;
};

export default AlertModalContainer;
